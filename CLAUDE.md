# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## ⚠️ REGRA Nº 1 — Mudar SÓ o que foi pedido

**Quando a roteirista pedir uma alteração, mexa APENAS no escopo exato do pedido.** Nunca aproveite pra "melhorar", refatorar, renomear ou remover outra coisa que já está funcionando. O que permanece dando certo, **mantenha intacto**.

- Antes de editar, isole o escopo exato. Se algo fora do pedido parecer errado, **pergunte antes de tocar** — não corrija por conta própria.
- Toda alteração vale para as **4 categorias** (`milionario-1p`, `milionario-3p`, `mafia`, `alpha-king`) salvo restrição explícita. Fazer nas 4 é *completar* o pedido, não extrapolar.
- Ao terminar, declare exatamente o que mudou e confirme que o resto ficou como estava.

**Invariantes provados — NÃO mexer sem pedido explícito:** Escrita P1‖P2 em paralelo + costura cruzada P1→P2 (`lib/generation/run-escrita.ts`, fonte única — não reanimar o branch morto do `StepShell`); Parte 2 = exatamente 6 capítulos nas 4 categorias (Parte 1 não mexer); calibração por-cap ±8% (Sonnet) + **balanço de total da Parte** (`balancePartTotal` em `run-escrita.ts` — se a SOMA da Parte cai fora de `partTotalRange`, expande os caps mais curtos/encurta os longos até entrar, mirando o MEIO da faixa; cada cap limitado ao próprio alvo, então a soma nunca cruza pro outro lado) + **piso de ~2.000 palavras/cap na Parte 2** (`enforceMinChapterShare` em `lib/balance-chapter-distribution.ts`, chamado por `normalizeEstruturaTargets` SÓ na Parte 2 — nenhum capítulo, em especial o ÚLTIMO, pode ficar minúsculo: eleva os curtos tirando das sobras dos longos, conserva a soma; **a Parte 1 fica de FORA**, tem caps curtos legítimos; bug "último capítulo de 800 palavras", 15/06/2026) / Revisor ±3%, anti-overshoot mira alvo×0,99 (subido de ×0,97 em 13/06/2026 — os caps vinham curtos e gastavam reescrita de calibração pra EXPANDIR; `milionario-3p` segue ×0,97 pois não subcontava; a mira ×0,94 ainda mais antiga deixava o total ABAIXO do piso); Revisor enxuto em todas as passadas (sempre NOTA + HATER + RISCO); narração em 1ª pessoa nas categorias de 1ª pessoa (`milionario-1p`, `mafia`, `alpha-king` — FMC nas duas Partes + MMC só nos trechos `✦` da P2, ZERO deslize pra 3ª pessoa/nome próprio da narradora; travada na Escrita E no Revisor — `milionario-3p` é a exceção, 3ª pessoa de propósito); **narrador invisível na `milionario-3p` — a palavra "narrador" (e "autor"/"escritor"/"narração"/"observador") JAMAIS pode virar SUJEITO/OBSERVADOR de uma frase na prosa, nem sutil no meio da oração (ex.: "e o narrador registrou o instante exato em que ele esqueceu a conversa", "um observador atento perceberia…"); o deslize se concentra na PARTE 2 ONISCIENTE (ter acesso às duas mentes NÃO autoriza anunciar a onisciência — onisciência é CONTAR o fato direto, "naquele instante ele esqueceu a conversa", nunca em nome de uma entidade); travado SÓ na `milionario-3p` (o narrador externo onisciente é EXCLUSIVO dela; as 3 de 1ª pessoa já barram terceira-pessoa/narrador via `_shared/narrator-knowledge-rule.ts`) na Escrita (`milionario3p/escrita-prompt.ts` — tique nomeado como "deslize nº 1 da Parte 2" na seção ONISCIENTE + regra de quarta parede + exemplos ❌/✅ com o rewrite direto) E no Revisor (`milionario3p/revisor-prompt.ts` §8 — entrada dedicada 💀 GRAVÍSSIMO, `trecho_corrigido` = prosa pura que CONTA o fato, sem citar a regra; não colide com `hasEditorialNote`/`stripCanonMeta`/`isNoOpCorrection`); **SEM sanitizer de código** — remover "o narrador registrou X" no meio da prosa exige reescrita gramatical, então só o Revisor limpa (código violaria a REGRA Nº 1); confirmado em roteiros reais do app oficial (9 ocorrências, 100% na P2); bug "vazamento do metadado o narrador", 5×/roteiro sempre na P2, 02/07/2026; Gravíssimo — **irmão brando (🟡, NÃO 💀 pra não travar o "pode finalizar"): "narrador didático"** = o onisciente COMENTA/EXPLICA por cima de uma ação já mostrada (etiqueta editorial "…com uma destreza que surpreendeu os dois"; afirmação chapada "Ele percebeu. Ele percebia tudo."), conserto = cortar a etiqueta OU filtrar pela percepção concreta de um personagem; **escopo APERTADO — interioridade onisciente legítima NÃO é erro, só o comentário REDUNDANTE**; travado em `escrita-prompt.ts` ("ONISCIÊNCIA MOSTRA, NÃO COMENTA") + `revisor-prompt.ts` §8**; **consistência de nome — uma identidade = UM nome canônico (o da 1ª aparição), a MESMA figura nunca pode ser referida por nomes diferentes (entre/within capítulos, mesma cena/frase, P1↔P2); travada na Escrita E no Revisor no bloco compartilhado `lib/agents/_shared/canone-rule.ts` (`CANONE_RULE` + `CANONE_REVISOR_CHECKLIST`, cobre as 4 categorias) — Revisor padroniza pela 1ª aparição (só com ALTA confiança de ser a MESMA figura) e, se a troca gerar o mesmo nome repetido na frase, emite erro INFORMATIVO (trecho_corrigido vazio) em vez de trocar por OUTRO personagem automaticamente (a troca automática bagunçava a cena — bug "Revisor troca nomes errado", 12/06); **o cânone/metadado JAMAIS entra na correção/prosa** — `trecho_corrigido` é prosa pura (proibido "cânone"/citação/tarja "CÂNONE DE ENTIDADES" **e nota editorial entre colchetes** — `[substitua X pelo nome do cânone…]`, `[NOME NÃO MENCIONE…]`), com defesa em código em `lib/parse-revisor-output.ts` (`stripCanonMeta` = tarja + citação endurecida SÓ nos cards; `stripCanonBanner` = só-a-tarja na prosa, pra nunca apagar "o cânone" substantivo legítimo; **`hasEditorialNote`/`stripEditorialNote` = colchete + marcador editorial: o parser ZERA o `trecho_corrigido` contaminado → card vira INFORMATIVO (roteirista decide, igual à colisão de nomes), `applyCorrections` NUNCA aplica (checa o RAW — `stripCanonMeta` comeria o "cânone de entidades" interno e esconderia a nota), e a prosa já contaminada é curada no heal/export via `sanitize-escrita-content.ts`; sweep `stripEditorialNote` ANTES de `stripCanonBanner`; bug "metadado do Revisor na correção" da 1.0.88, 12/06**); **correção no-op do Revisor (`trecho_original`≡`trecho_corrigido` — par idêntico, com a correção REAL enterrada no `por_que_alterado`/AVISO) JAMAIS vira card aplicável: `isNoOpCorrection` em `lib/parse-revisor-output.ts` (normaliza SÓ whitespace horizontal, PRESERVA quebras de linha, NÃO mexe em aspas/travessões — então substituição/inserção/troca-de-aspas/re-quebra-de-parágrafo legítimas NÃO casam) faz o parser ZERAR o `trecho_corrigido` → card INFORMATIVO (a roteirista lê o AVISO e decide), e a UI (`classifyError`/`renderCard` em `RevisorErrorsView.tsx`) repete o tratamento pra cards já salvos em 1.0.88 — acaba o botão "Aplicar" fadado a falhar e o "trecho não encontrado" mentiroso num trecho que EXISTE; o prompt do Revisor das 4 categorias proíbe o par igual e exige `trecho_original` = span EXATO que contém o erro / `trecho_corrigido` = esse span já corrigido (DIFERENTES); bug "trecho não encontrado num trecho que existe", 12/06**; Gravíssimo**; **tempo verbal — narração SEMPRE no passado (pretérito); só o diálogo (aspas/travessão) pode ficar no presente (exceção: verdades atemporais/aforismos no pensamento — "o amor é assim" — e ditados ficam no presente; o Revisor mira só nos verbos de AÇÃO/EVENTO da cena, pra não travar o "pode finalizar" à toa); bloco compartilhado `lib/agents/_shared/narration-tense-rule.ts` (`TENSE_RULE` na Escrita + `TENSE_REVISOR_CHECKLIST` no Revisor das 4 categorias) — é ortogonal à pessoa, então vale também pra `milionario-3p`; Gravíssimo**; **marcador de POV ✦ na Parte 2 — o NOME do marcador bate com QUEM narra o bloco logo abaixo (o exporter destaca o verde do MMC por esse nome; marcador trocado pinta o trecho errado e faz parecer outro narrador); UM bloco = UMA voz (o "eu" é a pessoa do marcador do início ao fim; a voz NUNCA troca no meio sem um novo `✦`); cena íntima/marcação = POV contínuo da heroína (nenhum `✦` do parceiro dentro, a voz dele nunca assume nem por uma frase); bloco compartilhado `lib/agents/_shared/pov-marker-rule.ts` (`POV_MARKER_RULE` na Escrita + `POV_MARKER_REVISOR_CHECKLIST` no Revisor) travado nas 3 categorias com POV alternado (`milionario-1p`, `mafia`, `alpha-king`) — **`milionario-3p` fica de FORA de propósito** (3ª onisciente, PROÍBE `✦`; o gate `revisao-rigorosa.mjs` confirma que ela não recebeu o bloco); ortogonal à 1ª pessoa e ao tempo verbal (fala de QUEM narra o bloco, não de pessoa/tempo); conserto do Revisor — marcador errado→troca SÓ o nome (a prosa é a verdade; só o nome muda, corpo intacto), voz sem marcador numa cena normal→insere `✦ NOME` (corrigido fica mais longo, não é no-op), cena íntima com voz dupla→reescreve a frase escorregada pro POV da heroína (NUNCA insere marcador do parceiro), dúvida→INFORMATIVO (trecho_corrigido vazio); não colide com `isNoOpCorrection`/`hasEditorialNote`/`stripCanonMeta`; bug 13/06/2026 (`✦ OSRIC` masculino sobre bloco da Solveig na alpha-king + `✦ Ottavia` feminino sobre bloco do Gennaro na máfia + voz trocando na cena de consumação sem marcador), **reforçado 15/06/2026 na revisão da história da Kai** (`✦ Teagan — POV feminino` encabeçando um bloco inteiramente narrado pelo Hudson — perspectiva e pensamentos dele; conserto = trocar SÓ o nome do marcador pra `✦ Hudson — POV masculino`, a prosa é a verdade, corpo intacto — o Revisor pegou e o card propôs exatamente a substituição; a roteirista reforçou: sempre respeitar o prompt mestre, sem desviar do padrão), **reforçado 16/06/2026 na revisão da história da Selena (alpha-king)** (bug "falta de cabeçalho de POV no retorno à heroína": o modelo trata a heroína como narradora-PADRÃO implícita e, depois de um bloco `✦` do parceiro, REINICIA o trecho dela SEM `✦ NOME` — o trecho da heroína fica órfão e parece continuação do POV dele; ex.: transição Osric→Solveig na P2 Cap. 3 sem `✦ Solveig`; o Revisor pegou como Grave e o card propôs inserir o marcador. Fix na ORIGEM: a heroína NÃO é narradora-padrão sem marcador na P2 — TODA troca de narrador leva `✦` NOS DOIS SENTIDOS (heroína→parceiro E parceiro→heroína) + a 1ª narração de cada cap da P2 também leva `✦` mesmo abrindo pela heroína; reforçado no `POV_MARKER_RULE` compartilhado + trazido o prompt da alpha-king ao nível do da máfia/milionário-1p, que já diziam "mesmo voltando o POV, identificação aparece novamente"; o `POV_MARKER_REVISOR_CHECKLIST` ganhou o bullet "troca de POV na fronteira do bloco SEM marcador nenhum — em especial o retorno à heroína", conserto = inserir `✦ NOME` antes do trecho órfão) — complementar e SEPARADO do fix de export das estrelas `✦ ✦ ✦` decorativas (lado da geração vs lado do export); Gravíssimo**; evals versionados (custo zero de cota); caching (system prompt via boundary + prefixo cacheável da Escrita; `cache_control` SÓ no último bloco); fila grava coalescido (`scheduleSave`, não `saveRoteiro` direto); `countWords` de `@/lib/word-count` como única fonte de contagem; streaming sempre throttled; nunca renomear a KEY `veludo:roteiros`; nunca abrir DevTools pro usuário; commit/push = release completo pra equipe (nunca `npm run release` local); **export — destaque verde SÓ do POV masculino (MMC) na Parte 2, a FMC NUNCA fica verde; rótulos `✦ NOME — POV feminino` / `— POV masculino`; quebra de cena (`---`/`***`/`___`/tarja `═━─`/**`✦` SOZINHO**) reseta o POV pra heroína — o `✦` sozinho é o separador da máfia/alpha-king e DEVE resetar, senão o verde do MMC vaza pra cena da heroína (bug recorrente da "Calla toda verde"); **anti-vazamento no PASTE do Google Docs (bug "roteiro todo verde ao copiar", 12/06/2026): TODO texto não-MMC (prosa da heroína + TODOS os títulos h1/h2/h3 + bullet) sai dentro de `<span style="background-color: #ffffff">` (helper `noHighlight`, `STYLE_NO_HIGHLIGHT`) — o Docs HERDA/RETÉM o destaque (de colagem anterior no doc OU do run do MMC) pros runs sem cor de fundo PRÓPRIA e pinta o roteiro inteiro, INCLUSIVE a P1 (que tem 0 verde no HTML) e os títulos. ⚠️ `transparent` NÃO resolve (o Docs ignora) — só BRANCO `#ffffff` (Docs honra como cor real e sobrescreve o verde herdado; invisível no papel branco). O HTML em si está CERTO (só o MMC verde) — o vazamento é no render do Docs; só o `CopyPartButton`→Docs sofre, o PDF/Chromium renderiza certo (`printBackground:true`, branco-sobre-branco invisível). `isMmcPov` (QUEM fica verde) INTACTO; embrulhar título num span NÃO quebra a navegação do Docs; NÃO reverter o wrap branco nem trocar por `transparent`**; a lógica do verde (QUEM fica verde) NÃO mudou desde a 1.0.81, então regressão de export = quase sempre FORMATO DE DADO (inspecionar o RAW/markers do roteiro real), não build/cache (`lib/export-html.ts`; validar com `node scripts/test-export-html.mjs`)**; **continuidade nas JUNÇÕES + marcador ✦ duplicado (v1.1.10, 08/07/2026): os graves/gravíssimos se concentram nas JUNÇÕES — entre POVs, entre uma cena e seu ECO/callback (reconstruído de memória) e entre Parte 1 ↔ Parte 2 (objeto-símbolo muda de cor/tipo, dia da semana muda, escada/lugar errado no epílogo, personagem age sobre info que ninguém lhe contou, fio de trama plantado e abandonado). Duas frentes: (a) guarda ADITIVA `lib/agents/_shared/junction-continuity-rule.ts` (`JUNCTION_CONTINUITY_RULE` na Escrita + `JUNCTION_CONTINUITY_REVISOR_CHECKLIST` no Revisor) nas 4 categorias (inclusive `milionario-3p` — continuidade é ORTOGONAL a pessoa/POV; concatenada após `NARRATOR_KNOWLEDGE_RULE` nas 3 de 1ª pessoa e após `TENSE_RULE` na 3p): objeto-símbolo (tipo+cor fixos nas duas Partes), linha do tempo (dia/hora/contagem), lugar/trajeto fiel no callback, estado físico contínuo (não acordar/desmaiar 2× sem transição), fala-chave dita UMA vez, CONHECIMENTO RASTREÁVEL (só age sobre info recebida em cena) e FIO DE TRAMA que se fecha — ADITIVA, NÃO reescreve estilo/pessoa/tempo/POV/nome; (b) trava determinística `lib/strip-duplicate-pov-markers.ts#stripDuplicateConsecutivePovMarkers` — colapsa marcadores `✦ NOME` IGUAIS adjacentes (mesmo nome, sem prosa entre eles = copiar-colar), mantendo o ÚLTIMO, SÓ na Parte 2 (na P1 remove TODOS via `stripPovMarkersPart1`; nomes DIFERENTES adjacentes NÃO são tocados — fica pro Revisor), em `run-escrita.ts` (ramo `else`/Parte 2) + warning `duplicatePovMarkersRemovedPart2` no `BatchWarningsBanner` + bullet no `POV_MARKER_REVISOR_CHECKLIST`; travado no gate `scripts/revisao-rigorosa.mjs` + `scripts/test-strip-duplicate-pov-markers.mjs`. É o Revisor que PEGA os erros de junção; a guarda faz a Escrita GERAR menos — prompt aditivo reduz, não zera (o resto é raciocínio do modelo + aplicar os cards)**.

## 🔍 Revisão rigorosa obrigatória (gate antes de concluir qualquer mudança)

**Toda mudança de código passa pelo subagente `revisor-rigoroso` ANTES de ser dada como pronta e ANTES de qualquer commit/push.** Ele é o portão que protege a produção das meninas — nenhuma alteração chega à equipe sem atravessá-lo.

- **Definição:** `.claude/agents/revisor-rigoroso.md` (subagente Opus). Invoque com a tool Agent, `subagent_type: "revisor-rigoroso"`, passando o escopo pedido + o diff.
- **Gate determinístico (1ª linha, instantâneo, sem cota):** `npm run revisao` → `scripts/revisao-rigorosa.mjs`. Varre o código e confirma as 7 REGRAS FIXAS travadas (cânone+tempo verbal na Escrita E no Revisor das 4 categorias; P2=6 capítulos; guard de metadado; KEY do storage). Sai 1 em qualquer reprovação.
- **Bateria completa que o subagente roda:** `npm run revisao` + `npm run typecheck` + `npm test` + `node scripts/test-export-html.mjs`, e depois a revisão semântica (POV/tempo verbal/nomes/metadado, REGRA Nº 1, cobertura das 4 categorias) que regex não pega.
- **Veredito:** APROVADO ou REPROVADO com cada violação em `arquivo:linha`. **REPROVADO bloqueia** — corrija e re-rode até passar antes de declarar concluído.

A regra de auto-invocação está em `Cerebro/_regras-fixas.md` (injetado em todo comando), então o gate se aplica sozinho a cada tarefa.

## What this is

Desktop app (Electron + Next.js 16) that drives a 5-step wizard for producing Brazilian Portuguese romance scripts ("Romance de Milionário"). Each step is a specialized Claude agent. **Auth is OAuth via the user's Claude Pro/Max subscription — there is no API key path in the runtime.**

## Commands

```bash
npm install
npm run electron:dev      # Next dev (port 3000) + Electron with hot-reload
npm run dev               # just Next.js, no Electron shell
npm run package           # next build + electron-builder NSIS installer (Windows)
npm run release           # same + publish to GitHub Releases (needs GH_TOKEN env)
npm run icon:build        # regenerates electron/icons/icon.ico from SVG (runs as `prepackage`)
```

There **are** quality scripts: `npm run typecheck` (`tsc --noEmit --incremental false`) and `npm test` (roda os `scripts/test-*.mjs` — parsers do Revisor, prompt-builder do cache, validate-rewrite). Os dois rodam no CI: `.github/workflows/ci.yml` em cada push/PR, e de novo no `release.yml` **antes** do build (portão de qualidade — erro de tipo/parser não vai mais pro release sem aviso). **Atenção:** os testes importam `.ts` direto, então exigem **Node ≥ 22** (em Node 20 quebram). Não há linter; `next build` segue como type-check final. Há também `npm run golden` (manual/local, **fora do CI** — precisa de OAuth + dev server rodando): roda **só o Revisor** sobre histórias fixas em `scripts/golden/*.json` e compara a NOTA/hate com a rodada anterior (`scripts/golden-eval.mjs`), pra pegar regressão quando você edita os prompts do Revisor sem regerar história (pouca cota). Veja `scripts/golden/README.md`.

## Architecture

### Three runtime modes (electron/main.js)

The Electron main process boots in one of three modes, decided in `boot()`:

1. **`external-dev`** — `NEXT_DEV_URL` is set (used by `npm run electron:dev`). Loads from the external Next dev server.
2. **`live`** — `MYSTORIESLENA_SOURCE_DIR` env var points at a valid project dir with `node_modules/next` installed. Spawns `next dev` from that source directory on a free port. This is the **maintainer-only mode**: the installed `MyStoriesLena.exe` reads directly from the working tree, so editing code + closing/reopening the app shows changes instantly. Auto-updater is disabled in this mode.
3. **`packaged`** — default for end users. Spawns `node server.js` from the standalone bundle in `process.resourcesPath/app` (copied there by `extraResources` in package.json), waits for `/api/health`, then loads it in the BrowserWindow.

### Claude binary resolution

The Claude Agent SDK shells out to a native `claude` binary (per-platform subpackage like `@anthropic-ai/claude-agent-sdk-win32-x64/claude.exe`). When packaged, those subpackages live outside `require.resolve` reach, so:

- `electron/main.js#getClaudeExecutablePath()` walks a list of candidate paths and picks the first that exists.
- The resolved path is passed to the Next server via `MYSTORIESLENA_CLAUDE_EXEC` env var.
- `lib/claude.ts` reads `process.env.MYSTORIESLENA_CLAUDE_EXEC` and passes it to `query()` as `pathToClaudeCodeExecutable`.

If you change anything about how the binary is bundled, the `extraResources` filter in `package.json` and the `subPaths` array in `getClaudeExecutablePath` must stay in sync. On Windows, the CLI also requires `bash.exe` from Git for Windows — `claude:setup` IPC autoinstalls it via winget.

### Env sanitization (critical)

`lib/claude.ts#streamClaudeText` deletes empty `ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN`, and any `ANTHROPIC_BASE_URL` from the env passed to the SDK. **Empty (not absent) values flip the SDK into API-key mode and cause 401s** even when the user is logged in via OAuth. Don't reintroduce these vars without similar guarding in `electron/main.js` (which does the same scrub before spawning the Next server).

### Streaming pipeline

`POST /api/agent/[step]` (`app/api/agent/[step]/route.ts`) is the single entrypoint:

1. Reads `category` from the body (default `"milionario-1p"` for legacy clients) and looks up the agent via `getAgent(category, step)` (`lib/agents/index.ts` → `lib/categories/index.ts`).
2. Calls `agent.buildUserMessage({ previousOutputs, userInput, referenceImage })` to assemble the prompt.
3. Uses `agent.model` directly. Models are passed as the SDK shorthand strings `"opus" | "sonnet" | "haiku"` (see `lib/anthropic.ts`), not full IDs — let the SDK resolve them.
4. If `agent.acceptsReferenceImage === true` and a `referenceImage` is present, decodes the data URL and sends a multimodal `[image, text]` user message; otherwise plain text.
5. Streams `content_block_delta.text_delta` chunks back as `text/plain`. The frontend (`components/wizard/StepShell.tsx`) reads with `ReadableStreamDefaultReader` and either updates the buffer live or, for the Escrita step, parses the structured output post-stream via `lib/parse-escrita-output.ts`.

If the SDK throws an auth-shaped error, the route injects a Portuguese-language `[LOGIN NECESSÁRIO NO CLAUDE]` block into the stream with recovery instructions.

### Categorias / sub-nichos

O app suporta múltiplos sub-nichos de romance, registrados em `lib/categories/index.ts`. Cada `Roteiro` tem um campo `category: RoteiroCategory` travado na criação (escolhido no `CategoryPicker` antes do roteiro existir):

- `milionario-1p` — Romance de Milionário em 1ª pessoa (default; default histórico para roteiros legados sem `category` — backfill em `lib/storage.ts`).
- `milionario-3p` — Romance de Milionário em 3ª pessoa pelo canal Rowan. Narração 3ª pessoa **limitada à FMC nas DUAS partes** (sem POV masculino, sem alternância — MMC observado pelos atos do início ao fim, inclusive na cena erótica da Parte 2), prompts próprios em `lib/agents/milionario3p/`. Premissa bifásica (resumos ≤500 palavras + obrigações de estrutura). Totais 10.500 (P1, faixa 10.000–11.000) / 13.500 (P2, faixa 13.000–14.000). Símbolos 🟢🟡🔴💀 no Revisor.
- `mafia` — Dark Romance de Máfia (prompts próprios, totais 12.500/13.500 em vez de 11.500/13.250, símbolos 🟢🟡🔴💀 no Revisor).
- `alpha-king` — Romance Alpha King / Werewolf pelo canal Alpha King (estilo Helô Stories™). Universo werewolf (alcateias, Moon Goddess, vínculo de mate, marcação, hierarquia Alpha/Luna/Beta/ômega). Narração 1ª pessoa: Parte 1 exclusiva da heroína (futura Luna); Parte 2 alterna heroína (principal) + 2–4 trechos do Alpha King marcados com `✦ NOME` — cena íntima/marcação completa sempre pela heroína (mesmo modelo de POV da máfia). Premissa bifásica (resumos ≤500 palavras + obrigações de estrutura). Totais 12.500 (P1, faixa 12.000–13.000, **6 capítulos fixos**) / 13.500 (P2, faixa 13.000–13.500, **6 capítulos fixos** — ver "Número de capítulos" abaixo). Símbolos 🟢🟡🔴💀 no Revisor. Prompts em `lib/agents/alphaking/`.

Cada categoria tem seu próprio diretório `lib/agents/<id>/` com 5 agentes + prompts. O dispatcher `getAgent(category, step)` em `lib/agents/index.ts` puxa do registry. `partTotalRange(part, category)` e `planBatches(..., category)` são category-aware. Os endpoints `/api/agent/[step]`, `/api/escrita-fix-wordcount` e `/api/revisor-extract-errors` recebem `category` no body — sem isso, caem no default `milionario-1p`.

Para adicionar um sub-nicho: crie `lib/agents/<id>/` (5 agentes + prompts), registre em `lib/categories/index.ts`, e estenda `RoteiroCategory` em `types/roteiro.ts`.

### Agent shape

Each step in `lib/agents/<categoria>/` exports an `Agent` (`lib/agents/types.ts`) with: `model`, `systemPrompt`, `buildUserMessage`, `thinking` (default `disabled` for speed), `effort` (default `low`), and `acceptsReferenceImage`. The Premissa step is intentionally manual in the UI — the user pastes text — but the agent definition exists for future use. **⚠️ `effort` SÓ tem efeito com `thinking: "adaptive"** (`lib/claude.ts` — com `thinking: "disabled"` o `effort` é um no-op). **O Revisor das 4 categorias é a EXCEÇÃO ao default de velocidade: roda `thinking: "adaptive"` + `effort: "medium"`** (08/07/2026) porque revisão é trabalho ANALÍTICO (cruzar cânone, continuidade P1↔P2/entre caps, POV, tempo verbal, furos de lógica) e o passe raso (disabled+low, onde o low era ignorado) deixava erros sutis vazarem pra revisão manual da roteirista. Não reverter pra `disabled`/`low` sem pedido — reintroduz o passe raso. O raciocínio é removido do texto salvo por `splitThinking` em TODO caminho (fila `run-step.ts`, foreground `StepShell` incl. refine/continuar-revisão), então NÃO vaza pra prosa/cards/XML. A Escrita segue `disabled` de propósito (geração criativa, tunada pra velocidade).

The Escrita agent runs in **2-em-2 mode**, driven by the headless engine `lib/generation/run-escrita.ts` (full-gen always enqueues — the `StepShell` loop is dead code for it). **Parte 1 e Parte 2 rodam EM PARALELO** (`Promise.all` de dois loops independentes: `[P1:1,2]→[P1:3,4]→…` ‖ `[P2:1,2]→[P2:3,4]→…`), cada Parte com seu próprio acumulador de sinopses pra continuidade **intra**-Parte. **Continuidade CRUZADA P1→P2 (costura na origem):** a P2 recebe a cada batch um snapshot **best-effort** das sinopses que a P1 **já escreveu** naquele instante (`crossSynopsesRef` no `runPartLoop` → `crossPartSynopses` no body → bloco "FATOS JÁ ESTABELECIDOS NA PARTE 1" via `lib/agents/_shared/cross-part-block.ts`, nas 4 categorias). Leitura **sem bloqueio** — como as duas Partes avançam no mesmo ritmo, a P2 normalmente já tem os fatos da P1 até o cap que está escrevendo, então o paralelismo continua ~cortando pela metade a fase de batches. Isso elimina **na origem** os GRAVES de continuidade cruzada ("personagem em dois lugares", "contradição de concepção P1↔P2") que a ponte só-pela-Estrutura deixava passar; a P1 é a fundação e não recebe contexto cruzado, e o Revisor (que também roda P1‖P2) ainda pega resíduos. Each batch returns the chapters plus a `═══ SINOPSES ═══` block with a 3-5 sentence summary per chapter — these synopses become context for subsequent batches **da mesma Parte**. **Assim que a geração de CADA Parte termina** (via `.then` no `Promise.all` — NÃO no fim das duas), aquela Parte dispara sua própria calibração de word-count **deferida e em PARALELO** (threshold/concorrência em `lib/escrita-calibration.ts` — hoje **±8%** e cap **5**, via `lib/concurrency.ts#createLimiter` — limitador **COMPARTILHADO** pelas duas Partes, então o teto **5** é o total mesmo com P1 e P2 alimentando o mesmo pool): cada cap fora da faixa vira uma reescrita **Sonnet** independente em `/api/escrita-fix-wordcount` (ajuste mecânico de tamanho — Sonnet alivia a fila Opus compartilhada da equipe) — **muta o capítulo in-place** no array da Parte e só lê sinopses **da própria Parte** (`s.part === ch.part`), então rodar concorrente dá o MESMO resultado que em série, só mais rápido. Como a P1 termina antes da P2, a **calibração (Sonnet) da P1 SOBREPÕE a cauda de geração (Opus) da P2** — aproveita a cota que ficava ociosa e tira a calibração do caminho crítico do "fim" (lossless: a vizinhança de sinopses já é por-Parte, então calibrar a P1 sem a P2 pronta não perde contexto). Antes a calibração era um passo único `mapWithConcurrency` **depois** das duas Partes — virou per-Parte sobreposta. O ±8% (afrouxado do ±5%) reduz o nº de reescritas (= mais rápido). **Logo após a calibração por-cap, cada Parte roda o BALANÇO DE TOTAL** (`balancePartTotal` em `run-escrita.ts`): se a SOMA da Parte cai fora de `partTotalRange`, expande os caps mais curtos (ou encurta os longos) via o MESMO `/api/escrita-fix-wordcount` até o total entrar na faixa, mirando o MEIO da faixa pra absorver a imprecisão do Sonnet — fecha o resíduo sistemático que o ±8% por-cap + a mira ×0,99 da geração deixavam (a SOMA caía abaixo do piso e NADA a puxava; a "fase 2 do Revisor" do design nunca existiu no código). Cada cap é limitado ao PRÓPRIO alvo (a soma dos alvos = alvo da Parte ≥ meio-da-faixa), então a soma nunca cruza pro outro lado nem estoura o teto individual; idempotente (Parte já na faixa → no-op), 2 passes (`BALANCE_MAX_PASSES`). A constante de calibração (`lib/escrita-calibration.ts`) é compartilhada; o full-gen vive SÓ em `run-escrita.ts` — o branch do `StepShell` é caminho morto, **não o reanime**. Numa assinatura única (OAuth), chamadas Opus concorrentes podem enfileirar no servidor — se a calibração paralela não acelerar, reduza `CALIBRATION_CONCURRENCY`. O motor headless emite o texto cru do batch atual via hook `onLiveText` (throttled ~80ms); o `QueueRunner` reflete em `useWizard.queueLiveStream` (transient) **só quando o roteiro do job está aberto**, e o `StepShell` mostra esse preview ao vivo no banner do job em 2º plano. The legacy parser `lib/parse-escrita-output.ts` is kept for retro-compat with old all-at-once roteiros in localStorage.

⚠️ **Word-count na ORIGEM (Escrita, 12/06/2026):** o ajuste de contagem — calibração por-cap (±8%, `calibratePart`) E **balanço de total da Parte** (`balancePartTotal` — puxa a SOMA pra dentro de `partTotalRange`, mira o MEIO da faixa) — roda na ESCRITA (`run-escrita.ts`), pra a roteirista já ver o total na faixa no "Capítulo gerado". O **Revisor step** hoje roda **só a revisão estruturada** (a fase 3 abaixo). As fases (1)/(2) a seguir são DESIGN HISTÓRICO de referência (faixas por categoria — milionário-1p 12.000-13.000/13.000-13.500, milionário-3p 10.000-11.000/13.000-14.000, máfia 12.000-13.000/13.300-13.700, alpha-king 12.000-13.000/13.000-13.500): (1) per-chapter extension via `/api/escrita-fix-wordcount` (±3%); (2) part-total balance se o total cai fora de `partTotalRange`; (3) structured review via `/api/agent/revisor` that streams the markdown report plus an `<erros_detalhados>` XML block parsed into `metadata.errors` for one-click `find+replace` fixes. Se o Revisor truncar/esquecer o XML, a UI dispara o fallback `/api/agent/revisor-extract-errors` que re-extrai o bloco `<erros_detalhados>` — **modelo Sonnet** (extração mecânica de XML; só re-localiza trechos que o Revisor já listou, não avaliação literária — Sonnet empata com Opus aqui e alivia a fila Opus da equipe). The escritaSnapshotHash is taken sobre o texto já calibrado/balanceado na Escrita, so the UI can detect post-revision edits. Re-clicking Gerar re-roda a revisão estruturada; o balanço de word-count (na Escrita) é idempotente — caps/totais já na faixa viram no-op.

**Revisor — modo enxuto SEMPRE (1ª passada inclusive):** **toda** revisão roda em **modo enxuto** (`leanRevisorReport`) — entrega `PRINCIPAIS ERROS` + o bloco `<erros_detalhados>` (que move os cards de 1 clique) + **sempre** a `ANÁLISE DE HATER`, o `NÍVEL DE RISCO DE HATE` e a `NOTA FINAL` (travados — a roteirista considera nota+hate essenciais), e **pula** o resto do ensaio (Sugestões Práticas / Análise como Leitor / Melhorias). Como só `<erros_detalhados>` + `NOTA` + `PRINCIPAIS ERROS` são consumidos por código (`lib/parse-revisor-output.ts`) e output domina o wall-clock, a roteirista optou por enxugar **já na 1ª passada** (antes só ligava da 2ª em diante) — ~corta o tempo da revisão. A instrução de override é injetada uma vez na factory `lib/agents/_shared/build-revisor-agent.ts` (cobre as 4 categorias, framing pass-agnostic); `lib/generation/run-step.ts#runRevisorStep` (fila — caminho da 1ª revisão) e o branch "Continuar revisão" do `StepShell` mandam **ambos** `leanRevisorReport: true` incondicional — **mantenha os dois em sincronia**. O ensaio completo deixou de ser gerado por padrão (pra reativá-lo seria preciso mandar `leanRevisorReport: false`; não há UI pra isso hoje).

**Revisor — Parte 1 e Parte 2 em paralelo:** o lock de concorrência do `QueueRunner` é por `${roteiroId}:${step}` (não por roteiro), então revisor1 e revisor2 do mesmo roteiro rodam ao mesmo tempo na fila (ainda limitado por `MAX_CONCURRENT_STREAMS`; cada job grava uma chave de step distinta e `applyStepOutput` re-lê o roteiro fresco e síncrono antes de gravar, sem race). O gate duro do revisor2 virou **aviso suave** — botão "Revisar as duas em paralelo" (a P2 roda sem o relatório da P1 como contexto; re-rodar a P2 enxuta depois de consolidar a P1 pega inconsistências cruzadas) + o caminho sequencial preservado.

### Evals de qualidade — nota objetiva versionada (context engineering / Karpathy)

Conceito do Karpathy "não dá pra otimizar o que não se mede" aplicado ao roteiro: a cada geração de revisão (revisor1/revisor2) o app deriva um **[EvalSnapshot]** (`types/roteiro.ts`) do relatório que o Revisor JÁ produz — **custo zero de cota, nenhuma chamada extra ao modelo**. Campos: `nota` (0–10, via `parseRevisorNota`), `hateRisk` (🟢/🟡/🔴, via `parseRevisorHateRisk` — heurística: nível seguido de travessão, fallback por emoji), `counts` por gravidade, `errorTotal`, `canFinish` (mesma regra do banner: Nota ≥ 8 e zero gravíssimos) e `escritaHash`. `computeRevisorEval` (`lib/parse-revisor-output.ts`) monta o snapshot; retorna null pra step não-revisor ou relatório sentinela (`[ERRO]`/`[LOGIN…]`).

- **Versionado, append-only:** gravado em `Roteiro.evals` (sobrevive ao round-trip do storage porque os sanitizers usam spread `{...r}`). `appendEvalSnapshot` (`lib/eval-log.ts`) anexa com **dedup** (re-commit idêntico — mesmo step+nota+hate+errorTotal+hash — não polui o log) e soft-cap `EVAL_LOG_CAP=200` só pra proteger o localStorage. Cresce além do cap de 5 do histórico de step, então dá pra ver a curva de qualidade ao longo de TODAS as re-rodadas.
- **Dois seams de gravação (mantenha em sincronia):** (1) foreground — `store/wizard.ts#recordEval`, chamado no `StepShell` SÓ nos dois pontos de conclusão de revisão (branch dedicado per-part + branch padrão de re-geração), **nunca** em refine/aplicar correção (o log é trilha de gerações, não de toda mutação); (2) fila — `QueueRunner#applyStepOutput` anexa no storage quando o roteiro NÃO está ativo (se ativo, o store grava via `recordEval` e seu persist venceria — evita corrida/duplicata). `lib/eval-log.ts` importa SÓ tipos de `@/types/roteiro` pra `appendEvalSnapshot` seguir testável via node (`--experimental-strip-types` não resolve `@/` em import de valor); a montagem que precisa de `computeRevisorEval` (import de valor) fica nos call sites.
- **UI:** `RevisorVerdictBanner` (`StepShell`) mostra Nota + **Δ vs a rodada anterior desta Parte** (▲/▼) + chip de risco de hate + um `<details>` "📈 Histórico de qualidade" com as últimas rodadas (nota, hate, nº de erros, ✅ pode-finalizar, tempo relativo). É advisory — nunca bloqueia.

### Escrita word counting — MANDATORY rule

**Always use `countWords` from `@/lib/word-count`.** This is the single source of truth — the same function the UI uses to display word counts in `WordCountBadge`. NEVER write a local `text.split(/\s+/)` counter — naive whitespace splits don't treat `—`, `–`, `-` as separators, which inflates counts by ~3% in romance text full of dialogue (`— Boa tarde.` is 2 words, not 3). Any divergence between backend counter and UI counter creates broken fix-wordcount/balance calls that ask for the wrong expansion.

**Número de capítulos — Parte 2 é SEMPRE exatamente 6 (todas as 4 categorias); Parte 1 fica como cada categoria já definia.** A Escrita gera tantos capítulos quantos a Estrutura declarar (`countChaptersInEstrutura` conta os cabeçalhos `Capítulo N`), então a trava vive nos prompts de Estrutura. **Parte 2:** cada `estrutura2-prompt.ts` (+ a mensagem AÇÃO em cada `estrutura2.ts`) diz "exatamente 6 — nunca menos, nunca mais". Antes a P2 dizia "entre 5 e 6", e quando o modelo escolhia 5 a Escrita gerava só 5 caps — por isso travou-se a P2 em 6 nas 4 categorias. **Parte 1 (NÃO foi mexida — a roteirista confirmou que as quantidades estão certas):** milionário-1p e alpha-king = exatamente 6; milionário-3p e máfia = "entre 5 e 6". NÃO reintroduzir "5 e 6"/"5-6"/"máximo 6" como contagem NA PARTE 2; as menções a "caps 5 e 6" que sobraram (P1 e P2) são o BLOCO FINAL (ritmo/posição da cena), não a contagem.

The structure-prompt rule (which `partTotalRange` in `lib/parse-estrutura-targets.ts` encodes) varies por categoria:
- **milionário-1p — Parte 1: 12.000–13.000 palavras totais** (alvo 12.500) — see `lib/agents/milionario1p/estrutura1-prompt.ts`
- **milionário-1p — Parte 2: 13.000–13.500 palavras totais** (rigoroso, jamais fora) — see `lib/agents/milionario1p/estrutura2-prompt.ts`
- **milionário-3p — Parte 1: 10.000–11.000 palavras totais** (alvo 10.500) — see `lib/agents/milionario3p/estrutura1-prompt.ts`
- **milionário-3p — Parte 2: 13.000–14.000 palavras totais** (alvo 13.500) — see `lib/agents/milionario3p/estrutura2-prompt.ts`

The per-chapter target lives in the structure header itself: `## Capítulo N — [Título] (~X.XXX palavras — ritmo Y)`. The `extractChapterTargets` parser in `lib/parse-estrutura-targets.ts` reads these per-cap targets directly from the headers — that's why `splitChapterBlocks` includes the header line in each block (the `(~X.XXX palavras)` is on the header, not in the body).

Per-chapter target margin is **±3%** (with a 30-word minimum) — see `targetRange`. After the last batch of each Part, a part-total compensating fix fires if the total falls outside the range.

**Trava de soma da Estrutura (determinística):** só a milionário-1p Parte 1 tem números fixos no prompt que somam o total exato; a Parte 2 e as outras categorias deixam o modelo distribuir as palavras (placeholders `~X.XXX palavras` / `Contagem aproximada: [N palavras]`) e costumavam **estourar** o total — a Escrita escrevia longo e a calibração/balanço tinham que encolher tudo (mais lento). Agora, após cada geração de Estrutura, `lib/normalize-estrutura-targets.ts#normalizeEstruturaTargets` soma os alvos por capítulo e, se a soma cair fora de `partTotalRange`, reescala proporcionalmente pro total alvo (resíduo de arredondamento no maior cap → soma EXATA), reescrevendo **só** o token de alvo de cada bloco de capítulo (não toca total/hook/prosa). Plugado em `run-step.ts#runEstruturaStep` (fila) e nos caminhos foreground de Estrutura no `StepShell` (continue/refine). Silencioso (`console.info`). Isso impede o estouro de chegar na Escrita → menos reescritas de calibração. **⚠️ Header-first nas DUAS pontas (bug "normalize infla a Estrutura", 12/06/2026 — Gravíssimo):** o alvo de cada capítulo vive no HEADER (`## Capítulo N — Título (~2.400 palavras)`); uma anotação de SUB-CENA no corpo (o trecho `✦` MMC da máfia/alpha-king, ex.: `"237-297 palavras"`) descreve um PEDAÇO, não o alvo. O LEITOR (`extractTargetFromBlock` em `parse-estrutura-targets.ts`) **e** o ESCRITOR (`rewriteTargetInBlock` em `normalize-estrutura-targets.ts`) leem/reescrevem o HEADER PRIMEIRO (fallback pro corpo só se o header não declarar alvo — ex.: milionário-3p) e **DEVEM ficar em sincronia**. Se um voltar a varrer o corpo primeiro, a faixa de sub-cena é lida como alvo (267 em vez de 2.400) → a soma parece baixa demais → o normalize reescala TUDO pra cima e a Parte estoura (foi o que inflou a P2 da máfia de 13.550 → 15.633). Travado no gate `scripts/revisao-rigorosa.mjs` (checa header-first nas duas funções). **Rodapé-resumo sincronizado (12/06/2026):** ao reescalar, a trava também reescreve a linha-resumo "**Soma das contagens declaradas:** A + B + … = TOTAL" pros alvos reescalados (`rewriteFooterSum`, SÓ essa linha-metadado — nunca a prosa). O modelo escreve esse resumo à mão e erra a conta (lista capítulos somando 15.500 e escreve "= 13.500"); depois do reescalonamento ficaria defasado de qualquer forma. A roteirista somava o rodapé na calculadora e achava que a contagem tinha estourado — mas a fonte real (header) já estava certa. Só reescreve se a qtd de números bater com o nº de capítulos; senão, no-op (texto intacto). NÃO é a fonte que a Escrita lê.

**Piso de DISTRIBUIÇÃO por capítulo na Parte 2 (bug "último capítulo de 800 palavras", 15/06/2026):** a Estrutura P2 só declarava o TOTAL (sem mínimo por cap) e o modelo às vezes fazia o ÚLTIMO capítulo um epílogo curto (~800 palavras, os outros ~2.300-2.900); como a SOMA seguia na faixa, `normalizeEstruturaTargets` (que só olhava a soma), `calibratePart` (cap-vs-próprio-alvo) e `balancePartTotal` (soma-da-Parte) deixavam passar — a Escrita gerava o cap minúsculo fiel ao alvo errado. Agora a MESMA `normalizeEstruturaTargets`, **SÓ na Parte 2** (`if (part === "Parte 2")`), também garante DISTRIBUIÇÃO mínima: `enforceMinChapterShare` (`lib/balance-chapter-distribution.ts`, puro/dep-free/testável via node) eleva qualquer cap abaixo do piso (`floor = round(alvoTotal/6 × MIN_SHARE_FRAC=0,90)` ≈ 2.000) tirando das sobras dos caps mais longos, **CONSERVANDO a soma** (que já está na faixa); idempotente. **Parte 1 fica de FORA de propósito** — tem caps curtos legítimos por desenho (ex.: alpha-king P1 Cap 6 ≤ 1.000), então a trava NUNCA roda nela. Reforçado nos 4 prompts da Estrutura P2 (`estrutura2-prompt.ts` + AÇÃO do `estrutura2.ts`: "MÍNIMO ~2.000 palavras por capítulo", "o último NÃO é um epílogo curto"), travado no gate `scripts/revisao-rigorosa.mjs` (sentinel do piso nos 4 prompts + `enforceMinChapterShare` + guard `part === "Parte 2"`) e validado por `scripts/test-normalize-estrutura-distribution.mjs` (no `npm test`). O `MIN_SHARE_FRAC` é um knob.

### State and persistence

- Wizard state lives in Zustand (`store/wizard.ts`) and is mirrored to `localStorage` under key `veludo:roteiros` via `lib/storage.ts` on every mutation.
- There is no server-side DB — the app is single-user, single-machine.
- Each step keeps a per-step history stack (max **2** snapshots, `HISTORY_CAP` exportado de `lib/storage.ts` — `store/wizard.ts` importa a MESMA constante, não repete o literal) so regenerating preserves the previous version. Baixado de 5 → 2 pra aliviar máquinas fracas (cada snapshot da Escrita ~200KB).
- **In-memory cache do storage:** `lib/storage.ts` mantém um cache (módulo-level) do array de roteiros já descomprimido + sanitizado. Sem ele, todo save (debounce 600ms + checkpoints a cada 2.5s no streaming) re-descomprimia e re-sanitizava a biblioteca inteira — O(biblioteca) por save. Agora o pipeline pesado roda só na 1ª leitura; os writers mutam o cache e só pagam o `serialize` (compress). Se algo escrever `veludo:roteiros` por fora dos helpers do módulo, chame `resetRoteirosCache()`.
- **Imagem de referência fora do blob quente:** o `dataUrl` (base64, ~0,3-0,5 MB) de `referenceImage` mora numa chave lateral `veludo:refimg:<id>`, NÃO no blob `veludo:roteiros` — senão entrava em TODA compressão de save (custo cresce com o nº de roteiros). Invariante: o cache em memória SEMPRE tem o `dataUrl` reidratado (`hydrateRefImage` no read); só o blob persistido grava `dataUrl: ""` (`stripRefImage` no write, via `serializeForBlob` em todos os writers; `serialize`/backup mantêm inline). `stripRefImage` só reescreve a chave quando a string muda (identidade `===` em `lastWrittenRefImg`), e cai pra inline se a quota estourar (nunca perde a imagem). Nenhum consumidor de `referenceImage` muda — todos leem do cache reidratado. Migração legada (imagem inline) é lazy: o 1º save move pra chave lateral.
- **Fila grava coalescido (anti-travamento):** o `QueueRunner` grava por id pelo caminho COALESCIDO/idle (`scheduleSave` ao longo do job + `requestPendingSaveFlush` ao concluir/falhar), NÃO o `saveRoteiro` direto. O save direto comprimia a biblioteca toda de forma síncrona a cada batch — com 2 jobs em paralelo isso empilhava e travava a janela (dialog "MyStoriesLena travou"). `scheduleSave` junta tudo num Map por id e comprime 1× por janela de 600ms em `requestIdleCallback`. `serialize` loga `[perf] serialize: <ms>ms, <chars> chars` — deve aparecer coalescido, não por batch×job.
- **Compressão FORA da main thread (anti-travamento, 1.0.89+):** a biblioteca cresceu (~8MB) e o `compressToUTF16` (lz-string) é síncrono ~1-2 MB/s — cada save virou bloqueio de segundos no renderer; numa geração longa os blocos se empilhavam e a janela não respondia ao ping do Chromium → diálogo "A janela parou de responder" (electron/main.js, dispara só após ~60s). **Fix:** o caminho QUENTE/idle (`performPendingSave`, via `scheduleSave`/`requestPendingSaveFlush`) comprime num **Web Worker inline (Blob URL)** com o `_compress` do lz-string **vendorizado** em `lib/lz-compress-worker-source.ts` (módulo sem deps, embutível no worker E testável via node — Blob inline em vez de `new Worker(new URL())` pra não depender de chunk emitido que some no `output: standalone`). A main thread só paga `JSON.stringify` + `setItem`. `performPendingSave` é async/coalescido (`saveInFlight`/`resaveQueued` = last-write-wins; re-checa `storageReadBlocked` pós-await; **fallback síncrono** se o worker estiver indisponível/erro/timeout 15s — save nunca se perde). **`flushPendingSave` continua 100% SÍNCRONO** (beforeunload/pagehide/troca de step/reset — durabilidade não pode esperar worker). `stripRefImage` fica na main thread (worker não tem localStorage). Leitura/`LZ1:`/KEY inalterados. Drift do compressor vendorizado travado por `scripts/test-lz-compress-vendored.mjs` (byte-a-byte vs npm, no `npm test`). Validar SEMPRE em build de produção. Deferido: mover `history` (55% do blob) pra chaves laterais como a imagem.
- **Limpar cache + limpeza automática:** botão "Limpar cache" (`components/ClearCacheButton.tsx`, no header da `RoteiroList`) + agente automático (`components/AutoCleanup.tsx` no layout, semanal via `veludo:lastCleanup`; + `runAutoCleanup` no main ~20s após o boot). Renderer: `pruneCacheLikeData()` poda `history`→2 / `evals`→30 e recompacta o blob enxuto — NUNCA toca em `outputs`/roteiros. Main (IPC `cache:clear`/`cache:get-size` em `electron/main.js`): `session.clearCache()` + `clearStorageData({ storages })` com **allowlist explícita SEM "localstorage"** (os roteiros moram lá — é o único ponto perigoso) + poda backups além de 5 + trunca `next-server.log`. Degradação graciosa sem Electron (só a poda do localStorage).
- **Backup automático (rede de segurança):** `components/LibraryBackup.tsx` (montado no layout) grava a biblioteca a cada ~5 min + ao esconder a janela em `userData/backups` (rotativo, mantém 5) via IPC `roteiros:auto-backup` — cópia FORA do localStorage. `RestoreBackupDialog.tsx` restaura. **Backup externo (fora da máquina):** botão "Backup externo" (`components/ExternalBackupButton.tsx`, header da `RoteiroList`) aponta pra uma pasta sincronizada (OneDrive/Drive) salva em `userData/backup-config.json` (IPC `backup:pick/get/clear-external-dir`); o auto-backup grava lá também um arquivo ROLANTE `veludo-roteiros-latest.json` (overwrite — mantém o Drive enxuto). Protege contra perda da máquina.
- **Métricas de throughput/cota (Pilar C — medir velocidade):** complementa os evals (que medem qualidade). `run-escrita.ts` dispara `onMetrics` no fim da geração; o `QueueRunner` persiste em `userData/metrics/perf.jsonl` (`lib/perf-metrics.ts` → IPC `metrics:append`/`metrics:read`, mantém 500 linhas). O painel `components/MetricsPanel.tsx` (botão "Métricas" no header da `RoteiroList`) mostra tempo/palavras/palavras-min/429/backoff por geração + o acumulado de cota da sessão (cache hit% e tokens) via `GET /api/claude-usage` (lê `getUsageTotals()` de `lib/claude.ts`). Só observabilidade — não muda a geração.

### Prompt caching (velocidade)

`lib/claude.ts#streamClaudeText` passa `systemPrompt` como `[systemPrompt, SYSTEM_PROMPT_DYNAMIC_BOUNDARY]` (constante do SDK). Tudo antes do marcador vira prefixo estático cacheável cross-call — como nosso system prompt é 100% estático por categoria/step, ele inteiro é reaproveitado nos 6+ lotes da Escrita, calibrações e fases do Revisor, **sem mudar o que o modelo lê**. O `cache_control: ephemeral` no user message (`buildPromptInput`) cobre re-runs idênticos em < 5 min. Confirme via `cache_read>0` nos logs `[claude.ts] usage:`. **Output domina o wall-clock** (ver `MEMORY.md`), então caching corta TTFT/cold mas não muda o tempo de geração em si.

**Prefixo cacheável na mensagem do usuário (context engineering / Karpathy):** além do system prompt, a mensagem do usuário da **Escrita** também tem um breakpoint de cache. O `buildUserMessage` dos 4 `escrita.ts` monta a mensagem como **prefixo estável** (cânone + premissa + estrutura1 + estrutura2, ~20k tokens — idêntico em todos os batches de uma geração) + o sentinela `CACHE_PREFIX_BOUNDARY` (`lib/agents/_shared/prompt-cache.ts`, U+0002; replicado inline em `lib/claude.ts`) + **sufixo variável** (intro do batch + sinopses + alvos + AÇÃO). O `buildPromptInput` quebra nesse sentinela em **dois text blocks**, AMBOS com `cache_control: { type: "ephemeral", ttl: "1h" }` — o prefixo passa a ser lido do cache (`cache_read`) nos 6+ batches da Parte e cruzado entre os loops P1‖P2, em vez de reprocessado a cada chamada. **⚠️ O `ttl: "1h"` EXPLÍCITO nos dois é obrigatório, não cosmético:** o Claude CLI injeta `ttl='1h'` no último bloco e a API rejeita (400) "um `ttl='1h'` não pode vir depois de um `ttl='5m'`" — deixar o prefixo com ephemeral default (5m) antes do 1h do CLI foi o que quebrou a Escrita na 1.0.74. Por isso **tudo é 1h** (sem nenhum 5m = ordenação impossível de violar). Travado pelos testes CASE 5/6 de `scripts/test-build-prompt-input.mjs`; ver `cache_control_ttl_ordering_trap` no `MEMORY.md`. **A intro do batch desceu pro sufixo** (era o topo) pra o prefixo estável liderar a mensagem (cache trabalha por prefixo de tokens) — por isso ela diz "estruturas ACIMA". Sem o sentinela (Revisor, Estrutura, calibração, refine), `buildPromptInput` mantém o bloco único (ephemeral default — é sempre o último bloco, sem 5m antes do 1h do CLI). Isso ataca o consumo de tokens na **cota compartilhada da equipe** (gargalo dominante) e o TTFT, **sem mudar o texto gerado**. O acumulador module-level em `lib/claude.ts` loga `[claude.ts] cumulative: … hit=NN%` — o hit% deve subir batch a batch; se um reorder quebrar o prefixo, `cache_read` despenca (regressão visível).

### Geração da Escrita — concorrente + persistente (gerenciador)

A **Escrita** roda num gerenciador FORA do componente: a geração **sobrevive a trocar/fechar a guia** e roda **concorrente** com outras abas (teto por **streams Opus**, não por jobs: `MAX_CONCURRENT_STREAMS = 4` + `jobStreamCost` = Escrita **2** (Parte 1 ‖ Parte 2) / todo outro step **1**. O teto 4 = o pico de 2 Escritas (2×2), então os steps leves de um 2º projeto rodam **JÁ** ao lado de uma Escrita pesada (1 Escrita=2 + 2 leves=2 = 4) **sem elevar** o pico de Opus na cota da assinatura compartilhada da equipe — antes o contador de **jobs=2** travava o 2º projeto inteiro. É um knob, afine pelos logs `[perf]`). Isso atende ao "deixar várias histórias gerando ao mesmo tempo".

- `store/queue.ts` — fila persistida em `veludo:queue` (só metadados leves; inclui um snapshot do `userInput` do job). Jobs `{ roteiroId, step: "escrita", status, progress, userInput? }`. Jobs `running` no fechamento voltam pra `queued` no load.
- `components/queue/QueueRunner.tsx` — montado 1× no layout. Drena jobs **em paralelo** até `MAX_CONCURRENT_STREAMS` streams Opus (Escrita=2, demais steps=1 via `jobStreamCost`; `loadRef` espelha o `runningRef`) — sem ceder ao foreground, sem 1-por-vez. Pra cada job: roda `runEscrita`, grava por id pelo caminho coalescido (`scheduleSave`, + `flushPendingSave` ao concluir — ver "Fila grava coalescido" em State and persistence) e, **se o roteiro do job for o aberto no momento, também reflete no store ativo** (`useWizard.setOutput`) pra a aba mostrar os capítulos surgindo. Notifica via `Notification` ao concluir.
- **Gatilho:** o botão "Gerar roteiro completo" da Escrita (`StepShell`) **enfileira no gerenciador** (não roda mais o loop no componente). Refine (correção pontual) continua no componente. O `RoteiroList` também tem botão "2º plano". `escritaJob` no `StepShell` mostra o progresso ao vivo + "Cancelar".
- `lib/generation/job-control.ts` — registro de `AbortController` por jobId (fica fora do store porque não é serializável); `abortJob`/`abortAllJobs` pra cancelar.
- **`lib/generation/run-escrita.ts` é o motor headless e a FONTE ÚNICA do full-gen** — roda **Parte 1 ‖ Parte 2 em paralelo** (`Promise.all` de dois `runPartLoop`), com retries, dedup (em DUAS camadas — ver abaixo), calibração por-cap ±8% + **balanço de total da Parte** (`balancePartTotal` fecha a SOMA dentro de `partTotalRange`; geração mira ×0,99) paralelos (Sonnet) via limitador compartilhado de `lib/concurrency.ts`, e logs `[perf]` por batch (TTFT/total/palavras) + contador de 429/backoff. O foreground in-componente do `StepShell` é caminho morto pra full-gen (interceptado antes — enfileira e dá `return`) e **não** tem a paralelização P1‖P2. **⚠️ Se mexer no loop da Escrita, mexa só no `run-escrita.ts`; não reanime o branch morto do `StepShell`.**
  - **Dedup de duplicação em DUAS camadas (cortar na ORIGEM, nunca deixar pro Revisor):** (a) **capítulo INTEIRO repetido** (dois cabeçalhos `## Capítulo N` com o mesmo número, ex.: retry re-emite o cap) → `dedupChaptersLast` (`lib/dedup-chapters.ts`, last-wins, keying por `parte|número`); (b) **bloco repetido DENTRO de UM capítulo** (o modelo "reinicia" no meio e re-emite um bloco grande de cenas SEM novo cabeçalho — "a segunda metade refaz a primeira") → `lib/strip-internal-duplication.ts#stripInternalDuplication`, chamado em `run-escrita.ts` logo após `parseEscritaBatch`, antes de acumular. A camada (b) era o BURACO que a roteirista reportava ("não consigo ver nos cards que duplicou, só a revisão acusa"): o cap é UM só (number N), o lixo está no CORPO, então `dedupChaptersLast` (que só olha o número) não enxergava e só o Revisor pegava ("Cap. N — bloco inteiro repetido"). **Alta precisão (trima PROSA — falso-positivo apaga cap bom):** só dispara no padrão inequívoco de restart — bloco de **≥2 parágrafos** + **≥200 palavras** + **match EXATO** (normalizado; não fuzzy — se o modelo parafrasear, NÃO toca, o Revisor pega o resíduo) + **2ª ocorrência até ~o fim do cap** (cauda ≤40 palavras) + **≥30% do capítulo** (separa restart real ~50% de carta/documento relido ~15%). Mantém a 1ª ocorrência, deleta da 2ª até o fim (a receita que o Revisor dava manualmente). Idempotente, puro/testável. As duas camadas registram **warning amarelo visível** (`duplicatesRemoved` / `internalDuplicateChapters` em `BatchMissingChapters`, renderizado no `BatchWarningsBanner`) — visibilidade > silêncio. **Validar:** `node scripts/test-strip-internal-duplication.mjs` (recall + precisão; ligado no `npm test`).
  - **Robustez "a Escrita JAMAIS termina com uma Parte vazia em silêncio nem pendura pra sempre" (NÃO remover):** (1) **watchdog de silêncio** no `readResponseText` — `Promise.race([reader.read(), stall])` com timer recriado a cada chunk; se NENHUM byte chegar por `STREAM_STALL_TIMEOUT_MS` (180s) o stream travado vira `StreamStallError` → cancela o reader e o loop trata como **transiente** (retry+backoff, conta como 429). Sem isso, um stream pendurado deixava o job "running" eterno (slot da fila preso, banner "batch N/N" infinito). (2) **Guard de completude** no fim — se uma Parte INTEIRA saiu com 0 capítulos (e não houve `abort`/fatal), lança `EscritaIncompleteError` em vez de retornar "pronto"; a fila marca o job `error` e a UI mostra "Continuar geração" (resume gera só o que falta). Gaps PARCIAIS (1-2 caps) seguem no caminho gracioso dos `batchWarnings`. (3) `QueueRunner` dá `flushPendingSave()` também no `catch` (durabilidade do parcial em falha).
- **Trade-offs:** (a) compartilha a cota da assinatura — N gerações ao mesmo tempo dividem o limite (cada uma pode ficar mais lenta); (b) a aba ativa mostra os capítulos **por par (batch)**, não streaming por token. **Estrutura e Revisor também rodam em 2º plano** (já feito): o `StepShell` intercepta o "Gerar" de `escrita + estrutura1/2 + revisor1/2 + overview` e enfileira no `QueueRunner` (genérico — `store/queue.ts` é step-agnóstico; `lib/generation/run-step.ts#runStreamingStep` despacha estrutura/revisor/overview). Sobrevivem a trocar/fechar a guia; só os modos pontuais (refine / "continuar de onde parou" / "continuar revisão" — este virou `continueRevisorPart`, que também enfileira) seguem foreground.

### Guias de projetos (multi-projeto)

Barra de abas pra alternar rápido entre projetos abertos, sem voltar à lista.

- `store/tabs.ts` — abas abertas `{ id, title }`, **persistidas em `veludo:tabs`** (a "memória" dos projetos simultâneos — voltam ao reabrir o app). `openTab` é upsert (cria ou atualiza título), cap `MAX_TABS=10` (FIFO).
- `components/tabs/ProjectTabs.tsx` — barra renderizada no topo do `Wizard` (some com ≤1 aba). Cada aba navega pra `/roteiro/<id>`, mostra spinner/ponto âmbar quando o roteiro está gerando/na fila (lê `useQueue`), e tem "×" pra fechar (se fechar a ativa, vai pra outra aba ou home). `+` abre/cria outro projeto.
- Guias são **só navegação** — trocar de aba navega (o store carrega aquele roteiro); não mantém N roteiros vivos em memória. A geração simultânea de fato é a **fila em 2º plano**. `Wizard.tsx` faz `openTab(id, title)` ao abrir; `RoteiroList` faz `closeTab` ao excluir.

### Electron ↔ renderer bridge

`electron/preload.js` exposes `window.mystorieslena` with: `getRuntimeInfo`, `checkForUpdates` / `downloadUpdate` / `quitAndInstall` (electron-updater), `exportRoteiroPdf` (uses Chromium `printToPDF` in a hidden BrowserWindow), `getClaudeStatus` / `setupClaude` (checks `~/.claude/.credentials.json` and opens a terminal running the bundled CLI for `/login`).

## Conventions and gotchas

- **Next.js 16 + React 19** with App Router, `output: "standalone"`. The bundled `node_modules/next/dist/docs/` is the source of truth for any framework API — see `AGENTS.md`.
- **Path alias `@/*`** maps to project root (see `tsconfig.json`).
- shadcn/ui components live in `components/ui/`; do not edit by hand if you can regenerate.
- All user-facing strings are **Brazilian Portuguese**. Match the existing tone when adding UI copy.
- The reference-image data URL is stored inline in the Roteiro (and thus in localStorage) — keep image size limits in mind when changing `ReferenceImageUpload`.
- Auto-updater is wired only when `app.isPackaged && runtimeMode === "packaged"`. Don't expect it to fire in dev or LIVE mode. **`autoUpdater.disableDifferentialDownload = true`** is intentional — o download diferencial (blockmap) dava `sha512 checksum mismatch` e deixava o instalador ausente (`Cannot run installer: ENOENT …Setup.exe`), fechando o app pra atualizar e não voltando. Não reverter pra diferencial.
- **🚪 O app NUNCA pode se fechar sozinho na máquina da roteirista (invariante de produção, 12/06/2026).** O app só encerra por: (a) a usuária fechar o X da janela (`window-all-closed → app.quit()`), (b) escolha explícita "Sair" no diálogo de boot-failure, ou (c) a 2ª instância do single-instance lock. **NUNCA** um `app.quit()` automático no caminho de crash: o diálogo de `render-process-gone` (`electron/main.js#createWindow`) só oferece "Recarregar" — não existe mais botão "Fechar app". **Single-instance lock** (`app.requestSingleInstanceLock`, só com `app.isPackaged`): 2ª instância (duplo-clique no ícone / relaunch do updater) foca a janela existente e sai, em vez de duas brigando pelo `veludo:roteiros`. Logs `[lifecycle]` em `window-all-closed`/`before-quit` cravam a causa de um fechamento inesperado no `main.log`. Causa de fundo provável = pressão de memória (renderer `render-process-gone {"reason":"killed"}`); ver `--max-old-space-size=8192`.
- When publishing (`npm run release`) the GitHub repo is hardcoded in `package.json#build.publish` (`lucasbaziliocomercial-crypto/mystorieslena`).
